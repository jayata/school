{% extends 'base.html.twig' %}

{% block body %}
    <h1>Matricula</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matricula.id }}</td>
            </tr>
            <tr>
                <th>Ano</th>
                <td>{{ matricula.ano }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('matricula_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('matricula_edit', { 'id': matricula.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
