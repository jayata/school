{% extends 'base.html.twig' %}
{% block title %}Cadastro{% endblock %}
     {% block stylesheets %}
         <link rel="stylesheet" href="{{ asset('css/bootstrap-datepicker.min.css') }}">
     {% endblock %}
 {% block body %}
     <div class="row">
     <div class="col-md-3"></div>
     <div class="col-md-6">

         <h3> Registre un novo aluno</h3>
         <form action="{{ path('admin_user_registration') }}"  method="POST">
                 <div class="form-group row">
                    <label for="exampleInputEmail1" class="col-md-3">Nome<code class="highlighter-red">*</code></label>
                     <input  name="name" required="required" maxlength="250" class="form-control col-md-9" placeholder="Digite um nome" type="text">
                        {% for message in app.flashes('error-input-name') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                     <label for="exampleInputEmail1" class="col-md-3">CPF<code class="highlighter-red">*</code></label>
                     <input name="cpf" required="required" maxlength="250" pattern="(\d{3}\.\d{3}\.\d{3}\-\d{2})" class="form-control col-md-9" placeholder="000.000.000-00" type="text">
                       {% for message in app.flashes('error-input-cpf') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                     <label for="exampleInputEmail1" class="col-md-3">RG<code class="highlighter-red">*</code></label>
                     <input name="rg" required="required" maxlength="250" class="form-control col-md-9" placeholder="Digite um RG" type="text">
                {% for message in app.flashes('error-input-rg') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                    <label for="exampleInputEmail1" class="col-md-3">Telefone<code class="highlighter-red">*</code></label>
                     <input name="telefone" required="required" maxlength="250" class="form-control col-md-9" placeholder="Digite um telefone" type="text">
                {% for message in app.flashes('error-input-telefone') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                     <label for="exampleInputEmail1" class="col-md-3">Data de nascimento<code class="highlighter-red">*</code></label>
                     <input name="dataNascimento" required="required" data-provide="datepicker" data-date-format="yyyy-mm-dd"
                      class="form-control col-md-9" value="{{ "now"|date("Y-m-d")  }}" type="text">
                {% for message in app.flashes('error-input-dataNascimento') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                     <label for="exampleInputEmail1" class="col-md-3">Username<code class="highlighter-red">*</code></label>
                     <input name="username" pattern=".{2,}" required="required" class="form-control col-md-9" type="text" placeholder="Digite um nome de usuário">
                {% for message in app.flashes('error-input-username') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>

                 <div class="form-group row">
                      <label for="exampleInputEmail1" class="col-md-3">E-mail<code class="highlighter-red">*</code></label>
                     <input name="email" required="required" class="form-control col-md-9" placeholder="Digite um email" type="email">
                     </small>
                     {% for message in app.flashes('error-input-email') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div class="form-group row">
                     <label for="exampleInputEmail1" class="col-md-3">Senha<code class="highlighter-red">*</code></label>
                     <input name="plainPassword" required="required" class="form-control col-md-9" placeholder="Digite uma senha" type="password">
                {% for message in app.flashes('error-input-plainPassword') %}
                        <div class="alert alert-danger alert-dismissible fade show alert-admin-add-user" role="alert">
                            {{ message }}
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    {% endfor %}
                 </div>
                 <div>
                     <input class="btn btn-lg btn-primary btn-block" type="submit" value="Cadastrar">
                 </div>
         </form>

     </div>
     <div class="col-md-3"></div>
 {% endblock %}
         {% block javascripts %}
    <script src="{{ asset('js/bootstrap-datepicker.min.js') }}"></script>
{% endblock %}